"""Cross-implementation and on-network interop (Phase 1).

- embit_signer: signer B implemented on embit, an independent,
  well-reviewed codebase (used by SeedSigner) — proves our PSBTs are
  consumable and co-signable by another implementation.
- signet_interop: live end-to-end run on the signet test network.
"""
